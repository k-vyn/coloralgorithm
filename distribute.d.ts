interface Props {
    value: number;
    rangeA: number[];
    rangeB: number[];
}
declare type Result = number;
export default function distribute({ value, rangeA, rangeB }: Props): Result;
export {};
